# Password gate (Java)

A console version of the [password-locked page note](../../password-locked-section/).

Password: `try-me`

This is a gate, not a vault. Anyone who opens the `.java` file can see the check.

[Try the browser demo](https://micah-masuka.github.io/Learning/java/password-gate/) — no JDK needed.

## Run

```bash
javac PasswordGate.java
java PasswordGate
```
